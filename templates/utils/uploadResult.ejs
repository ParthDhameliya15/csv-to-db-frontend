<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Result</title>
</head>
<body>
    <h1>Upload Result</h1>
    <% if (newRecords.length > 0) { %>
        <h2>New Records:</h2>
        <ul>
            <% newRecords.forEach(record => { %>
                <li>Email: <%= record.email %>, Created At: <%= record.createdAt %></li>
            <% }); %>
        </ul>
    <% } %>
    
</body>
</html>
